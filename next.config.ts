import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", "images.unsplash.com", "azis-agantal-blog.vercel.app"],
  },
};

export default nextConfig;
